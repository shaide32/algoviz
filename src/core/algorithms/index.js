import { metaData as sortingMetaData } from './sorting';

const algos = [];
algos.push(sortingMetaData);

const meta = {
    name: '',
    url: '',
    children: algos
}

export {
    meta as metaData
}