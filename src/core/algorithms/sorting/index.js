import {metaData as bubbleMetaData} from './bubbleSort';
const algos = [];
algos.push(bubbleMetaData);

const meta = {
    name: 'Sorting',
    url: '/sorting',
    children: algos
}

export {
    meta as metaData
}